# agentic-flow
A workflow engine for multi-agent systems where agents execute tasks based on dependency graphs.  Think of it like Airflow for AI agents.  Developers can define tasks, dependencies, and agents that execute them.
